subscription OnSupersededStatus {
  Status(context: "superseded") {
    commit {
      sha
      message
      statuses {
        context
        description
        state
      }
      repo {
        ...CoreRepoFieldsAndChannels
      }
    }
    state
    targetUrl
    context
    description
  }
}
