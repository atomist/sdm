subscription OnRequestedSdmGoal($goalName: String!, $environment: String!) {
  SdmGoal(
    state: ["requested"]
    name: [$goalName]
    environment: [$environment]
  ) {
    ...SdmGoalFields
    ...SdmGoalRepo
  }
}
