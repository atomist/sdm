
subscription OnDeployableArtifact {
    Status(state: success, context: "artifact") {
        commit {
            sha
            message
            statuses {
                state
                targetUrl
            }
            repo {
                owner
                name
                channels {
                    name
                    id
                }
            }
            pushes(branch: "master") {
                branch
            }
        }
        state
        targetUrl
    }
}

