query InProcessSdmGoalSets($fetch: Int!, $registration: [String!]) {
  SdmGoalSet(_orderBy: "ts", _first: $fetch) {
    goalSetId
    state
    goalSet
    ts
    provenance(registration: $registration) @required {
      registration
    }
  }
}
