query PushForSdmGoal(
  $owner: String!
  $repo: String!
  $providerId: String!
  $branch: String!
  $sha: String!
) {
  Commit(sha: $sha) {
    pushes(branch: $branch) {
      ...PushFields
    }
  }
}
