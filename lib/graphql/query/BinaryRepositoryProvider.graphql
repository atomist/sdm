query BinaryRepositoryProvider($name: String, $type: BinaryRepositoryType) {
  BinaryRepositoryProvider(name: $name, type: $type) {
    url
    name
    state {
      error
      name
    }
    credential {
      owner {
        login
      }
      id
    }
  }
}
