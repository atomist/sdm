query DockerRegistryProvider($name: String) {
  DockerRegistryProvider(name: $name) {
    url
    name
    state {
      error
      name
    }
    credential {
      owner {
        login
      }
      id
    }
  }
}
